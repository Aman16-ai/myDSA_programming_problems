#include<iostream>
#include<conio.h>
int main() {
	char[10] strOriginal;
	char[10] strReverse;
	std::cout<<endl<<"Enter the string";
	std::cin>>str;
	for(int i =0,j=strlen(str);i<strlen(str);i++,j--) {
		strReverse[i] = strOriginal[j];
	}
	std::cout<<endl<<"String after reverse"<<strReverse;
      	std::cout<<endl<<"Press any key to continue...";
	getch();
	return 0;
}
		
